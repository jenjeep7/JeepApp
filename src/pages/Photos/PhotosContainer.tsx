import React from 'react';
import PhotosPresentation from './PhotosPresentation';

const PhotosContainer: React.FC = () => {
    return (
        <PhotosPresentation />
    )
}

export default PhotosContainer;